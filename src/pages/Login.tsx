export default function Login() {
  return (
    <div className="flex-col h-screen w-full flex items-center justify-center bg-primary-light">
        <h2 className="text-3xl font-bold text-center text-secondary mb-8">Login</h2>
        <a href="/" className="text-secondary hover:text-secondary-dark transition-colors">Voltar</a>
    </div>
  );
}